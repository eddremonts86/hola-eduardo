# AI Providers

Esta carpeta agrupa la infraestructura de providers AI. El registro compartido y el model discovery viven en el nivel superior, y cada provider expone su configuracion base desde su propia carpeta para que la navegacion sea evidente.

Providers disponibles:

- `anthropic`
- `lmstudio`
- `llama-cpp`
- `ollama`
- `openai`
