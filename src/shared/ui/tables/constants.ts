/** Default items per page for infinite queries */
export const DEFAULT_PAGE_SIZE = 20

/** Estimated row height (px) for useVirtualizer */
export const VIRTUAL_ROW_HEIGHT = 52

/** Extra rows rendered outside the visible viewport */
export const VIRTUAL_OVERSCAN = 10

/** Minimum characters before search activates */
export const SEARCH_MIN_CHARS = 2

/** Debounce delay (ms) for search input */
export const SEARCH_DEBOUNCE_MS = 300

/** Toast duration (ms) for delete confirmation */
export const DELETE_TOAST_DURATION = 10_000
